namespace ControlStatements
{
    class Program
    {
        static void Main(string[] _)
        {
            /* Control statements are used to control the program execution flow. Used to make execution flow "jump forward" or "jump
             * backward".
             * Conditional Control Statements: if(simple,else,nested-if)
             * Looping Control: while | do-while | for
             * Jumping Control: goto | break | continue
             */
        }
    }
}
